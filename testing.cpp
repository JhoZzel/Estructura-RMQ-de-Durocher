#include <bits/stdc++.h>
#include "SparseTable.h"
#include "random.h"
using namespace std;

const int MAX_N = 10000 + 4;
const int MAX_V = 1e9;

int main() {
 
    int n = 5e5;
    int k = ceil(0.5 * log(n) / log(4));
    cout << k << endl;

    return 0;
}

